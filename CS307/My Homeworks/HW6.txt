Joshua Wright CS307 HW6

Sorts from N-1 to 0: 

template <typename T>
void randperm(vector<T> &v) {
	for (int i=(int)v.size()-1; i>0; --i) {
		swap(v[i], v[rand() % (i+1)]);
	}
}

Sorts from 0 to N-1:

template <typename T>
void randperm(vector<T> &v) {
	for (int i=0; i<(int)v.size()-1; ++i) {
		swap(v[i], v[rand() % (((int)v.size()-1) - (i-1)) + (i-1)];
	}
}