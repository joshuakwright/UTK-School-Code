#include<iostream>
#include<cstdlib>
using namespace std;

int main() {
	int random = rand();
	// No srand() calls before rand(), so seed = 1
	cout << "Seed = 1, Random number = " << random << endl;

	srand(-123);
	// Seed = 5 
	random = rand();
		cout << "Seed = -123, Random number = " << random << endl;

/*
	int myarray[5];
	int index = 2;
	myarray[index] = 5;
	cout << myarray[index];
	return 0;
*/
}
